rpi-servo is an app for controling a servo motor on GPIO pin 18 of a Raspberry Pi, using a multi-platform GUI.

Run both the client and server using run.bat (Windows) or run.sh (Linux)
You can test it without a Raspberry Pi, as the server displays output to the command line